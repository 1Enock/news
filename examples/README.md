# Examples and Solutions

Place runnable example code and small demos here. Recommended layout:

- `examples/python/` — Python example scripts and notebooks
- `examples/data/` — small sample datasets (gitignored if large)

Each example should include a short `README.md` and a `run.sh` or `requirements.txt` when needed.
